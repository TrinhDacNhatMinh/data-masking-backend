package com.minh.data_masking.util;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;

import java.util.Base64;

@Service
public class AesService {

    private static final int[] SBOX = {
            
    };

    private static final int[] INV_SBOX = {
            
    };

    private static final int[] RCON = {0x8d, 0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1b, 0x36};

    private final int[][] expandedKey;

    public AesService(@Value("${aes.secret-key}") String secretKey) {
        byte[] keyBytes = secretKey.getBytes();
        int[] key = new int[16];
        for (int i = 0; i < 16; i++) key[i] = (i < keyBytes.length) ? (keyBytes[i] & 0xFF) : 0;
        this.expandedKey = keyExpansion(key);
    }

    // ==================== KEY EXPANSION ====================

    private int[][] keyExpansion(int[] key) {
        int[][] expanded = new int[44][4];
        for (int i = 0; i < 4; i++) {
            expanded[i][0] = key[4 * i];
            expanded[i][1] = key[4 * i + 1];
            expanded[i][2] = key[4 * i + 2];
            expanded[i][3] = key[4 * i + 3];
        }
        for (int i = 4; i < 44; i++) {
            int[] temp = {expanded[i - 1][0], expanded[i - 1][1], expanded[i - 1][2], expanded[i - 1][3]};
            if (i % 4 == 0) {
                int t = temp[0];
                temp[0] = temp[1];
                temp[1] = temp[2];
                temp[2] = temp[3];
                temp[3] = t;
                for (int j = 0; j < 4; j++) temp[j] = SBOX[temp[j]];
                temp[0] ^= RCON[i / 4];
            }
            for (int j = 0; j < 4; j++) expanded[i][j] = expanded[i - 4][j] ^ temp[j];
        }
        return expanded;
    }

    // ==================== AES OPERATIONS ====================

    private int gmul(int a, int b) {
        int p = 0;
        for (int i = 0; i < 8; i++) {
            if ((b & 1) != 0) p ^= a;
            boolean hiBitSet = (a & 0x80) != 0;
            a = (a << 1) & 0xFF;
            if (hiBitSet) a ^= 0x1b;
            b >>= 1;
        }
        return p;
    }

    private void addRoundKey(int[][] state, int round) {
        for (int c = 0; c < 4; c++)
            for (int r = 0; r < 4; r++)
                state[r][c] ^= expandedKey[round * 4 + c][r];
    }

    private void subBytes(int[][] state, int[] box) {
        for (int r = 0; r < 4; r++)
            for (int c = 0; c < 4; c++)
                state[r][c] = box[state[r][c]];
    }

    private void shiftRows(int[][] state) {
        int t;
        t = state[1][0];
        state[1][0] = state[1][1];
        state[1][1] = state[1][2];
        state[1][2] = state[1][3];
        state[1][3] = t;
        t = state[2][0];
        state[2][0] = state[2][2];
        state[2][2] = t;
        t = state[2][1];
        state[2][1] = state[2][3];
        state[2][3] = t;
        t = state[3][3];
        state[3][3] = state[3][2];
        state[3][2] = state[3][1];
        state[3][1] = state[3][0];
        state[3][0] = t;
    }

    private void invShiftRows(int[][] state) {
        int t;
        t = state[1][3];
        state[1][3] = state[1][2];
        state[1][2] = state[1][1];
        state[1][1] = state[1][0];
        state[1][0] = t;
        t = state[2][0];
        state[2][0] = state[2][2];
        state[2][2] = t;
        t = state[2][1];
        state[2][1] = state[2][3];
        state[2][3] = t;
        t = state[3][0];
        state[3][0] = state[3][1];
        state[3][1] = state[3][2];
        state[3][2] = state[3][3];
        state[3][3] = t;
    }

    private void mixColumns(int[][] state) {
        for (int c = 0; c < 4; c++) {
            int[] col = {state[0][c], state[1][c], state[2][c], state[3][c]};
            state[0][c] = gmul(0x02, col[0]) ^ gmul(0x03, col[1]) ^ col[2] ^ col[3];
            state[1][c] = col[0] ^ gmul(0x02, col[1]) ^ gmul(0x03, col[2]) ^ col[3];
            state[2][c] = col[0] ^ col[1] ^ gmul(0x02, col[2]) ^ gmul(0x03, col[3]);
            state[3][c] = gmul(0x03, col[0]) ^ col[1] ^ col[2] ^ gmul(0x02, col[3]);
        }
    }

    private void invMixColumns(int[][] state) {
        for (int c = 0; c < 4; c++) {
            int[] col = {state[0][c], state[1][c], state[2][c], state[3][c]};
            state[0][c] = gmul(0x0e, col[0]) ^ gmul(0x0b, col[1]) ^ gmul(0x0d, col[2]) ^ gmul(0x09, col[3]);
            state[1][c] = gmul(0x09, col[0]) ^ gmul(0x0e, col[1]) ^ gmul(0x0b, col[2]) ^ gmul(0x0d, col[3]);
            state[2][c] = gmul(0x0d, col[0]) ^ gmul(0x09, col[1]) ^ gmul(0x0e, col[2]) ^ gmul(0x0b, col[3]);
            state[3][c] = gmul(0x0b, col[0]) ^ gmul(0x0d, col[1]) ^ gmul(0x09, col[2]) ^ gmul(0x0e, col[3]);
        }
    }

    private void encryptBlock(int[][] state) {
        addRoundKey(state, 0);
        for (int round = 1; round < 10; round++) {
            subBytes(state, SBOX);
            shiftRows(state);
            mixColumns(state);
            addRoundKey(state, round);
        }
        subBytes(state, SBOX);
        shiftRows(state);
        addRoundKey(state, 10);
    }

    private void decryptBlock(int[][] state) {
        addRoundKey(state, 10);
        for (int round = 9; round > 0; round--) {
            invShiftRows(state);
            subBytes(state, INV_SBOX);
            addRoundKey(state, round);
            invMixColumns(state);
        }
        invShiftRows(state);
        subBytes(state, INV_SBOX);
        addRoundKey(state, 0);
    }

    // ==================== PUBLIC API ====================

    public String encrypt(String plainText) {
        byte[] bytes = plainText.getBytes();
        int paddedLength = (bytes.length / 16 + 1) * 16;
        byte[] paddedBytes = new byte[paddedLength];
        System.arraycopy(bytes, 0, paddedBytes, 0, bytes.length);

        byte[] encryptedBytes = new byte[paddedLength];
        for (int i = 0; i < paddedLength; i += 16) {
            int[][] state = new int[4][4];
            for (int r = 0; r < 4; r++)
                for (int c = 0; c < 4; c++)
                    state[r][c] = paddedBytes[i + r + 4 * c] & 0xFF;
            encryptBlock(state);
            for (int r = 0; r < 4; r++)
                for (int c = 0; c < 4; c++)
                    encryptedBytes[i + r + 4 * c] = (byte) state[r][c];
        }
        return Base64.getEncoder().encodeToString(encryptedBytes);
    }

    public String decrypt(String encryptedText) {
        byte[] bytes = Base64.getDecoder().decode(encryptedText);
        byte[] decryptedBytes = new byte[bytes.length];
        for (int i = 0; i < bytes.length; i += 16) {
            int[][] state = new int[4][4];
            for (int r = 0; r < 4; r++)
                for (int c = 0; c < 4; c++)
                    state[r][c] = bytes[i + r + 4 * c] & 0xFF;
            decryptBlock(state);
            for (int r = 0; r < 4; r++)
                for (int c = 0; c < 4; c++)
                    decryptedBytes[i + r + 4 * c] = (byte) state[r][c];
        }
        return new String(decryptedBytes).trim();
    }

}
